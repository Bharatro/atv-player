# Release 0.99.0

## 新增

- 播完全屏相关推荐:播放列表最后一集正常播完后,视频区弹出全屏网格海报卡片(与列表页同规格,最多 24 张,列数随窗口宽度自适应),点击即可换播并自动续播;按 Esc、点击 ✕、切换选集或开始新播放时自动关闭。
- 推荐区块标题自定义:内容源可通过 `vod_related_label` 自定义推荐区块标题(默认"相关推荐")。

## 优化

- 相关推荐协议统一:同系列与相似推荐合并为 `vod_related` 字段,条目支持封面和年份,推荐卡片展示更完整。
- 详情侧栏推荐胶囊升级:标题随源自定义,当前播放条目高亮,点击同窗换播;折叠/展开偏好统一记忆,支持"默认展开/默认折叠/跟随上次"三态。
