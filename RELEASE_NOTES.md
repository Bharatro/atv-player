# Release 0.100.0

## 新增

- 章节自动跳过片头片尾:识别视频章节后自动跳过片头/片尾,默认开启,可在设置中关闭。
- B站视频章节:识别 B站视频章节(view_points),播放器支持查看章节并点击跳转。
- B站详情互动按钮:详情页新增点赞/投币/收藏按钮(需登录 B站账号),取代原本地追更/收藏。
- 弹幕屏蔽词支持正则:屏蔽词支持 `/正则/flags` 格式,规则更灵活。
- 首页插件管理菜单:首页插件 tab 右键菜单支持插件自定义管理动作。

## 优化

- 网盘起播提速:弹幕加载让位于直链解析,弹幕源整轮并发,此前最长 41 秒的起播等待显著缩短。
- TMDB 接入改为 Worker 轮询池,并补发 X-TMDB-API-Key 请求头,刮削更稳定、兼容新式 Worker。
- CENC 加密视频代理地址改为扁平路径(`/cenc/{token}.mp4`),提升播放器兼容性。
- 播放窗口左右边缘预留窗口调整热区,减少视频误触。

## 修复

- 详情页站内跳转先回到主窗口,内联链接样式与 BVID 对齐。
- 优酷弹幕 cna 获取失败时本地生成兜底,弹幕不再缺失。
- 搜狐弹幕搜索适配新版接口签名,并修复播放列表解析。
