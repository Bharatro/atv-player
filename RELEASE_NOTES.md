# Release 0.88.2

## 新增

- 支持启动时优先加载自定义 libmpv：将可用的 libmpv 放到用户目录下的 `mpv` 目录或应用目录下的 `lib` 子目录即可自动优先加载，解决部分老 CPU 机器上内置 libmpv 无法加载导致播放报错的问题；指定文件加载失败时会记录日志并自动回退到内置 libmpv。

## 优化

- 完善自定义 libmpv 加载：候选按目录与文件名优先级逐个尝试，单个文件加载失败会自动尝试下一个，全部失败才回退内置；新增支持把 DLL 直接放到应用目录（exe 旁边）。

## 修复

- 解决自定义 `libmpv-2.dll` 可能被 PATH 中其它 `mpv-2.dll` 按名字优先级抢先、仍加载到内置版本的问题，现在会自动处理，无需手动改名。
