## 新增

- 新增网络代理设置页，支持直连、系统代理以及手动配置 HTTP / HTTPS / SOCKS5 代理。
- 新增直连规则配置，默认绕过本地地址和局域网网段，便于在代理环境下兼顾本地服务访问。

## 优化

- 优化代理生效范围，API、元数据、解析源、弹幕、海报、插件下载、HLS 上游请求和 `yt-dlp` 现已统一遵循代理设置。
- 优化代理决策逻辑，可按目标地址自动判断走代理还是直连，减少手动切换。

## 修复

- 修复部分网络请求链路未继承代理配置的问题，使用代理时各类在线资源访问表现更一致。
