# Release 0.95.0

## 新增

- Linux 版播放内核更智能：启动时在系统与内置 libmpv 中自动选择较新版本加载，依赖不匹配的发行版上不再因内置库加载失败而无法播放；也可将自定义 libmpv 放到 `~/mpv` 或应用 `lib` 目录优先使用。按 F1 查看帮助时可看到实际加载的 libmpv 来源与路径。

## 修复

- 修复管理员账号仅配置一个 VOD token 时，登录仍弹出选择框的问题，现在直接进入应用。
